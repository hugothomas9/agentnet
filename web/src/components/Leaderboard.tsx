/**
 * Tableau de classement des agents
 * Props : LeaderboardEntry[]
 * Colonnes : rang, nom, score (%), taux completion, volume, diversite
 * Tri et filtrage cote client
 */
export function Leaderboard() {
  // TODO: implementer
  return <table></table>;
}
