/**
 * Page detail d'un agent — /agent/[id]
 * - Infos NFT (nom, version, owner, wallet, capabilities, endpoint)
 * - ReputationChart (graphe des metriques)
 * - Historique des delegations (ActivityLog)
 * - Lien Solscan vers le NFT
 */
export default function AgentDetailPage({
  params,
}: {
  params: { id: string };
}) {
  // TODO: implementer
  return <main></main>;
}
