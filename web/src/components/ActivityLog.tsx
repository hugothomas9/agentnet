/**
 * Log en temps reel des delegations entre agents
 * Props : DelegationLog[]
 * Affiche : timestamp, from → to, tache, montant, statut
 * Mise a jour en temps reel (polling ou websocket)
 */
export function ActivityLog() {
  // TODO: implementer
  return <div></div>;
}
