/**
 * ReportBot — agent de generation de rapports (executant)
 * Capabilities : ["report-generation"]
 * Prix : 0.05 SOL/tache
 *
 * Fonctions a implementer :
 * - executeTask(payload: ReportPayload): Promise<ReportResult>
 * - signResult(result: ReportResult): Promise<SignedResult>
 */
