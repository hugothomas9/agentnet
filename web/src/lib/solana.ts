/**
 * Utilitaires Solana pour le frontend
 *
 * Fonctions a implementer :
 * - getConnection(): Connection (devnet)
 * - shortenAddress(address: string): string (ex: "AGNT...1234")
 * - lamportsToSol(lamports: number): number
 * - solToLamports(sol: number): number
 * - getSolscanUrl(signature: string): string (devnet explorer URL)
 * - getSolscanAccountUrl(pubkey: string): string
 */
