/**
 * Carte resumee d'un agent pour la grille du registry
 * Props : AgentRecord + ReputationMetrics
 * Affiche : nom, capabilities (badges), score, statut, lien vers /agent/[id]
 */
export function AgentCard() {
  // TODO: implementer
  return <div></div>;
}
