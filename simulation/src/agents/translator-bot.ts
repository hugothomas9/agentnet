/**
 * TranslatorBot — agent de traduction (executant)
 * Capabilities : ["translate"]
 * Prix : 0.02 SOL/tache
 *
 * Fonctions a implementer :
 * - executeTask(payload: TranslatePayload): Promise<TranslateResult>
 * - signResult(result: TranslateResult): Promise<SignedResult>
 */
