/**
 * ResearchBot — agent de veille concurrentielle (demandeur)
 * Mission : analyser la concurrence fintech Japon + rapport PDF
 * Budget : 0.5 SOL
 *
 * Fonctions a implementer :
 * - decomposeMission(): SubTask[] — decompose la mission en sous-taches
 * - findAgents(capability: string): Promise<AgentRecord[]> — cherche dans le registre
 * - delegateTask(agent: AgentRecord, task: SubTask): Promise<DelegationResult>
 * - assembleFinalReport(results: DelegationResult[]): void
 */
