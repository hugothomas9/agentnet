/**
 * Page Leaderboard — classement des agents par score
 * - Tableau tri par score decroissant
 * - Filtres par capability, volume minimum
 * - Colonnes : rang, nom, score, taux completion, volume, diversite
 */
export default function LeaderboardPage() {
  // TODO: implementer
  return <main></main>;
}
