/**
 * Formulaire d'enregistrement d'un nouvel agent
 * Champs : nom, version, capabilities (multi-select), endpoint URL
 * Action : connecte Phantom, appelle POST /agents/register
 */
export function RegisterForm() {
  // TODO: implementer
  return <form></form>;
}
