/**
 * Logger style terminal pour la demo
 * Affiche les etapes avec timestamps, couleurs et formatage
 *
 * Fonctions a implementer :
 * - logStep(step: string, detail: string): void — affiche une etape
 * - logTransaction(tx: string, description: string): void — affiche une TX avec lien Solscan
 * - logMetrics(agent: string, metrics: Partial<ReputationMetrics>): void — affiche les metriques
 * - logResult(summary: DemoSummary): void — affiche le bilan final
 */
