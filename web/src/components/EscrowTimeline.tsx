/**
 * Frise chronologique d'un escrow (3 transactions)
 * Props : EscrowRecord
 * Affiche : TX1 (creation) → TX2 (soumission) → TX3 (liberation/refund)
 * Chaque etape avec statut, timestamp, lien Solscan
 */
export function EscrowTimeline() {
  // TODO: implementer
  return <div></div>;
}
